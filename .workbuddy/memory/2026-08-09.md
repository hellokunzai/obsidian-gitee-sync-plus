# 2026-08-09

- Git 面板「更改」组标题按钮顺序调整：按用户截图要求，将 `+`（全部暂存）与 `↩`（放弃全部）顺序对调，改为 `↩` 在前、`+` 在后。实现：src/GitPanelView.ts 中 renderGroup 的 DOM 创建顺序从 stageAll/unstageAll/discardAll 改为 discardAll/stageAll/unstageAll。构建通过，产物已拷到 D:\WorkSprace\gitee\build，git 提交 `bf6c5fa`。
- 差异对比「还原块」按钮改为始终显示：按用户截图，差异视图中每块首行的 ↩ 还原按钮原本只在鼠标悬停行时显示，现改为默认 opacity:1 常驻可见；移除 `.gitee-sync-plus-diff-row:hover .gitee-sync-plus-diff-revert-btn` 规则，保留按钮自身 hover 高亮。仅修改 styles.css，构建通过，产物已拷到 D:\WorkSprace\gitee\build，git 提交 `a85ebf8`。
- 还原块按钮图标改为左箭头并放大：DiffView.ts 图标文本从 `↩` 改为 `←`；styles.css 按钮尺寸 18px→20px、字号 12px→14px。构建通过，产物已拷到 D:\WorkSprace\gitee\build，git 提交 `d773b77`。
- 还原块按钮改为圆角长方形：styles.css 中 border-radius:50%→4px、尺寸 20x20→22x18，与面板图标按钮风格一致。git 提交 `dd88471`。
- 还原块按钮加宽且悬停不透明：宽度 22px→28px、高度 18px→20px；hover 规则显式 opacity:1 确保鼠标悬停时不透明。git 提交 `9155b17`。
- 差异对比视图布局切换改为跟随屏幕方向 + 手动覆盖：用户反馈希望横屏后显示为左右并排结构（截图）。实现：默认 `auto` 模式，竖屏(portrait)自动使用上下堆叠布局，横屏(landscape)自动使用左右并排布局；`layoutMode` 支持 auto/side-by-side/stacked 三态，按钮点击循环切换，允许手动覆盖；监听 `orientationchange` 事件，auto 模式下方向变化自动重绘。i18n.ts 新增 diffLayoutAuto/diffLayoutAutoHint 中英文案。构建通过，产物已拷到 D:\WorkSprace\gitee\build，git 提交 `a095d2b`。
- 差异视图改为双文件完整视图：用户上传竖屏/横屏两张示意图，明确要的是「远端版本」「本地版本」两个完整文件独立显示，而不是把同一 diff 行的 old/new 塞进同一行。实现：DiffView.ts 中移除 renderSideBySideDiff/renderStackedDiff，合并为 renderDiff + renderFileSection，分别渲染 before/after 两个 section；每个 section 内 table 显示完整文件所有行，带连续行号，差异行按状态着色（删除红/新增绿/修改橙）；横屏时左右 flex 并排，竖屏时上下 flex 堆叠。styles.css 新增 .gitee-sync-plus-diff-file-wrapper / .gitee-sync-plus-diff-file-section / .gitee-sync-plus-diff-file-label，调整 .gitee-sync-plus-diff-content 默认 flex-direction:row、.layout-stacked 时 column；移除旧的 labels/stacked 样式。还原块按钮保留，优先放在本地版本（after）差异块首行，远端删除块则放在远端版本（before）首行。构建通过，产物已拷到 D:\WorkSprace\gitee\build，git 提交 `99dbd60`。
- 回滚差异视图到「布局切换（左右/上下）」版本：用户决定放弃双文件完整视图及跟随屏幕方向自动切换，要求回滚到「点击切换为横屏时只是编辑框从左右布局改为上下布局」的状态。执行 `git reset --hard 6ef7e2d`，丢弃 `a095d2b` 与 `99dbd60` 两个提交；重新 `npm run build` 并通过；产物 main.js / manifest.json / styles.css 已重新拷到 D:\WorkSprace\gitee\build。当前 HEAD 为 `6ef7e2d`。
- 修复 PC 端差异对比上下布局宽度问题：用户截图显示上下布局时内容区只占据左侧一半，右侧留白。根因是 `.gitee-sync-plus-diff-cell` 默认 `width: 50%`（用于左右并排模式），在上下堆叠模式下每个 section 的表格仍为两列（行号 + 内容），导致内容列仅占 50%。修复：styles.css 中为 `.gitee-sync-plus-diff-stacked-section` 增加 `width: 100%`，并为 `.gitee-sync-plus-diff-stacked-table .gitee-sync-plus-diff-cell` 设置 `width: 100%`，使内容列占满表格剩余宽度。构建通过，产物已拷到 D:\WorkSprace\gitee\build，git 提交 `6ab4187`。
- 用户要求回滚到 `9155b17`（还原块按钮加宽且悬停不透明）。执行 `git reset --hard 9155b17`，丢弃 `09452de`/`6ef7e2d`/`a095d2b`/`99dbd60`/`6ab4187` 等后续提交（即放弃差异对比布局切换功能与上下布局宽度修复）。重新 `npm run build` 通过，产物已重新拷到 D:\WorkSprace\gitee\build（**未 git 提交**）。当前 HEAD 为 `9155b17`。
- 全局记忆新增死命令：未经用户明确说"git 提交"，绝对不允许执行任何 git commit（已写入 `~/.workbuddy/MEMORY.md`「用户偏好」）。
- 差异对比视图新增横竖向切换按钮：用户截图显示当前为左右并排差异视图，要求在顶部新增按钮切换为上下布局。实现：DiffView.ts 中新增 `layout: "side-by-side" | "vertical"` 状态，header 右侧增加 actions 区域和布局切换按钮；点击按钮切换 `layout` 并给 diffContentEl 添加/移除 `layout-side-by-side` / `layout-vertical` class；按钮文本用 `⇄`/`⇅` 区分当前模式与可切换目标，title 动态更新。i18n.ts 新增 `diffLayoutSideBySide`/`diffLayoutVertical`/`diffLayoutToggle` 中英文案。styles.css 中调整 header 为 flex 布局，新增 `.gitee-sync-plus-diff-actions`/`.gitee-sync-plus-diff-layout-btn` 样式；vertical 模式下 table/tr/td 转为 flex column，旧版/新版内容上下堆叠，纯删除/纯插入行隐藏空 counterpart 列，change 行在两版本间加分隔线。构建通过。按死命令未提交 git。
- 用户要求将构建产物打包放到 `D:\WorkSprace\gitee\build`。重新 `npm run build` 通过，并拷入 main.js（113KB）、manifest.json、styles.css 三个文件至该目录。未 git 提交。
- 用户截图要求竖向布局为「远端版本」「本地版本」两个完整文件上下排列，而非单 diff 行的上下堆叠。实现：重写 DiffView.ts 的 `renderDiff`，改为渲染 before/after 两个完整文件 section；计算每行状态（equal/delete/insert/change），分别渲染连续行号的完整文件表格；横向时 section 左右 flex 并排，纵向时上下 flex 堆叠。styles.css 替换旧 diff table 样式为 `.gitee-sync-plus-diff-files`/`.gitee-sync-plus-diff-section`/`.gitee-sync-plus-diff-section-scroll`/`.gitee-sync-plus-diff-section-table`/`.gitee-sync-plus-diff-section-label`，并区分 before 侧（delete/change 红色）与 after 侧（insert/change 绿色）背景色。保留还原块按钮，放在每差异块在对应 section 首行的行号列内。构建通过，产物已重新拷到 `D:\WorkSprace\gitee\build`。未 git 提交。
- 用户明确要求 git 本地提交。只暂存源码三文件（src/DiffView.ts、src/i18n.ts、styles.css，main.js 不入库），提交成功，commit `1cb811a`，message：`feat(diff): 差异视图布局切换按钮 + 双文件完整视图`。未 push。
- 用户参考 JetBrains diff 截图，要求在差异视图顶部新增两个按钮：收起未更改的片段、同步滚动。实现：DiffView.ts 新增 `collapseUnchanged` 与 `syncScroll` 状态及对应切换按钮（布局按钮旁）；收起功能在 `renderFileSection` 中把连续未更改行超过 4 行的区段中间部分折叠，保留前后各 2 行上下文，生成占位行显示 "N 行未更改"，点击占位行可展开；父容器 `.collapse-unchanged` class 控制显示/隐藏。同步滚动在 `attachScrollSync` 中给两个 section 的 scrollBox 监听 scroll，按滚动比例同步对侧 scrollTop。i18n.ts 新增 diffCollapseUnchanged/diffExpandUnchanged/diffSyncScroll/diffSyncScrollOff/diffUnchangedLines 中英文案。styles.css 新增 `.gitee-sync-plus-diff-action-btn` 通用按钮样式与激活态，新增折叠相关样式。构建通过，产物已拷到 `D:\WorkSprace\gitee\build`。未 git 提交。
- 用户再次参考 JetBrains diff 截图，要求把差异视图改为「删除红 / 新增绿 / 修改橙」并使用空白占位行对齐两侧内容位置。实现：重写 DiffView.ts 的 `renderDiff`，横向模式改为单 table 统一渲染，每行包含旧版/新版两个 cell，删除时新版 cell 空白、新增时旧版 cell 空白、修改时两侧都有内容；纵向模式仍为「远端版本 / 本地版本」两个 section，但按 diff rows 同步插入空白占位行，使两侧行数一致、内容位置对齐。DiffView.ts 新增 `buildRowSegments`、`renderSideBySide`、`renderSideBySideSegment`、`renderSideBySideRow`、`renderVertical`、`renderVerticalSegment`、`renderVerticalRows` 辅助方法。styles.css 删除 change 行绿色背景，改为 `.state-change` 橙色背景；新增 `.state-placeholder` 占位行样式与 `.gitee-sync-plus-diff-table`（单 table 横向布局）样式；新增 `.gitee-sync-plus-diff-section-labels` 与 `.gitee-sync-plus-diff-section-wide` 样式。构建通过，产物已拷到 `D:\WorkSprace\gitee\build`。未 git 提交。
- 修复纵向布局切换失效：上一步重写后 DOM 结构随布局不同而不同（横向单 table、纵向双 section），但 `toggleLayout` 只切换 CSS class 未重新渲染，导致点击切换后布局不生效。修复：DiffView.ts 新增 `lastDiff` 缓存（path/beforeText/afterText/beforeLabel/afterLabel），`renderDiff` 末尾赋值，`toggleLayout` 切换到新 layout 后调用 `renderDiff` 用缓存参数重新渲染。构建通过，产物已重新拷到 `D:\WorkSprace\gitee\build`。未 git 提交。
- 差异视图还原按钮覆盖删除/新增/修改：用户要求删除、新增、修改三类差异行都应带还原按钮。原实现只把按钮放在每个差异块首行且仅放在 after（本地版本）侧，删除行（after 侧空白）与插入行显得没有按钮。修改：`renderSideBySideRow` 与 `renderVerticalRows` 改为基于 `row.chunkIndex` 判断，凡是属于差异块的行，就在「该行有内容的那一侧」放还原按钮——删除行放远端（before）侧、插入行放本地（after）侧、修改行两侧都放。`chunkFirstRowIndex` 不再需要，已从 renderDiff/renderSideBySide/renderVertical 及其 segment 方法中移除。构建通过，产物已重新拷到 `D:\WorkSprace\gitee\build`。未 git 提交。
- 删除差异视图的「同步滚动」按钮，改为横向/纵向默认都开启同步滚动：用户要求去掉该按钮并默认开启。实现：DiffView.ts 删除 `syncScroll` 字段、`toggleSyncScroll` 与 `updateSyncButton` 方法、header 中的 syncBtn 创建、`renderDiff` 末尾的 `this.updateSyncButton()` 调用及 `if (this.syncScroll)` 条件；改为 `renderDiff` 末尾无条件 `this.attachScrollSync()`。`attachScrollSync`/`detachScrollSync` 保留（detach 在每次重写前调用以防重复监听）。i18n.ts 删除 `diffSyncScroll`/`diffSyncScrollOff` 中英文案。CSS 中同步按钮复用通用 `action-btn` 样式、无专属规则，`is-active` 仍被收起按钮使用，无需改动。构建通过，产物已重新拷到 `D:\WorkSprace\gitee\build`。未 git 提交。
- 差异视图还原按钮图标和提示语按用户截图调整：图标从 `←` 改为 `↩`（与截图一致）；提示语从「还原此更改块」改为「放弃」，英文从 "Revert this change block" 改为 "Abandon"。实现：DiffView.ts 中两处还原按钮 text 替换为 `↩`；i18n.ts 修改 `diffRevertChunk` 中英文文案。构建通过，产物已重新拷到 `D:\WorkSprace\gitee\build`。未 git 提交。
- 用户要求 git 本地提交。将累积的差异视图改动一并提交（之前多轮"未 git 提交"的源码改动合并入库）。commit `5cc7393`，message：`feat(diff): 差异视图着色/对齐优化，还原按钮覆盖三类并默认开启同步滚动`。包含 src/DiffView.ts、src/i18n.ts、styles.css 三文件（+314/-141）。涵盖：删红/增绿/改橙着色与占位行对齐、三类还原按钮、删除同步滚动按钮并默认开启、还原按钮图标↩及提示语「放弃」、纵向切换失效修复。未纳入 .workbuddy/、CHANGELOG.md、outputs/（未跟踪）。未 push。
- **侧边栏按钮可配置显示/隐藏**：为 `SyncSettings` 新增 `showSyncRibbon` 和 `showPanelRibbon` 两个布尔设置项（默认 `true`，向后兼容），在设置面板末尾添加了两个 toggle 开关。`main.ts` 中 ribbon 图标改为按条件创建，并新增 `updateRibbonIcons()` 方法支持运行时动态切换（切换开关即时生效，无需重启）。i18n 已补充中英文。构建通过，未 git 提交。
- **提交 Obsidian 社区市场前合规审计与修复**：通读 src/ 全部源码核对社区插件规范。修复：(1) `githost.ts` 的 `gitBlobSha1` 原依赖 `crypto.subtle.digest`，移动端 webview（非安全上下文）可能为 undefined 导致同步崩溃（插件 `isDesktopOnly:false`）→ 改用自包含纯 JS `sha1`（git blob 格式），已用 `git hash-object` 校验空/hello/Unicode/1MB 全部一致；(2) `versions.json` 补 `"1.0.1":"1.8.7"` 与 manifest 对齐；(3) `DiffView.ts` 的 `app:any`→`App`，新增 `setDiffPluginInstance` 单例避免依赖未公开 `app.plugins.getPlugin`；(4) `settings.ts` 分支下拉框 `dropdownComponent:any`→`DropdownComponent`，`selectEl.innerHTML=""`→`selectEl.empty()`；(5) `README.md` 中英文「排除目录」表格行更新为 `.gitignore` 原始规则（原「逗号分隔前缀」已过时）。`npm run build` 通过。`main.js` 已重新构建（经 Release 资产分发，仓库内通常不入库）。改动 6 文件，未 git 提交（等用户明确要求）。
- **发布 v1.0.2（SSH → GitHub hellokunzai）**：bump `manifest.json` 1.0.1→1.0.2、`versions.json` 补 `"1.0.2":"1.8.7"`、CHANGELOG.md 追加 1.0.2 条目；`npm run build` 通过；精确暂存 8 文件（README.md / src/DiffView.ts / src/githost.ts / src/main.ts / src/settings.ts / versions.json / manifest.json / CHANGELOG.md），排除 `.workbuddy/` 与 `outputs/`；commit `1ed18af`；`git tag 1.0.2`；`git push github main` + `git push github 1.0.2` 均成功（SSH 别名 `github.com-hellokunzai` → `ssh.github.com:443`）。tag 推送触发 `.github/workflows/release.yml` 自动构建并发布 Release（main.js / manifest.json / styles.css）。gh CLI 未安装，Actions 运行状态需到 GitHub 仓库 Actions 标签页确认。
- **清理 versions.json 的 1.0.0 测试版本映射**：用户手动在 GitHub 删除了 1.0.0 draft release，随后明确要求 git 提交。将 `versions.json` 的 `"1.0.0":"1.8.7"` 删除（仅保留 1.0.1/1.0.2）；仅暂存 versions.json（排除 `.workbuddy/`、`outputs/`）；commit `babfc37`；`git push github main` 成功（`1ed18af..babfc37`）。本地/远程均无 1.0.0 tag（1.0.0 从未打 tag，仅 GitHub draft release 已删）。
- **撰写 1.0.2 公众号文章**：按 outputs/ 内 1.0.0 文章叙事风格，总结 1.0.0→1.0.2 这轮「IDE 级版本控制」功能（侧边栏 Git 面板、DiffView 差异对比、暂存区与选择性提交、侧边栏按钮可配置、设置分组、1.0.2 移动端纯 JS SHA-1 + DiffView 实例加固 + 版本映射对齐 + CI 自动构建），写入 `outputs/gitee-sync-plus-1.0.2-公众号文章.md`。纯文案交付，未改动代码、未 git 提交。
- **合并两篇公众号文章为功能总览**：保留 `outputs/` 下 1.0.0 两篇原稿不动，新增 `outputs/gitee-sync-plus-功能总览-公众号文章.md`，将 1.0.0 起源故事 + 1.0.2 IDE 级版本控制 + README 中的引擎/多设备/多触发/双语/iOS 可见性等全部功能合并为一篇完整功能详解。纯文案交付，未改代码、未 git 提交。
