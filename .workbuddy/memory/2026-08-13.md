# 2026-08-13

## 实现选择秘钥对话框与测试按钮

- 新增 `src/token-manager.ts`：插件维护命名秘钥列表（`tokenProfiles`），每个秘钥值存入 Obsidian `secretStorage`，key 前缀 `gitee-sync-plus-token-`。
- `src/settings.ts`：
  - 私人令牌/访问令牌输入框改为「选择秘钥」+「测试」按钮。
  - 新增 `TokenSelectModal`：搜索、单选、添加、查看/隐藏、删除、保存。
  - 测试按钮调用 `githost.testToken()` 验证当前秘钥。
- `src/main.ts`：初始化 `TokenManager`，加载数据时自动迁移旧版单令牌到命名秘钥。
- `src/githost.ts`：新增 `testToken()`（复用 `fetchBranches`）。
- `src/i18n.ts` / `styles.css`：补充中/英文案与对话框样式。
- 版本号升至 `1.1.0`（manifest / package / versions）。
- `README.md` / `CHANGELOG.md` 同步更新。
- 构建通过，安全扫描无禁用 API。

注意：`SecretStorage` 没有 `deleteSecret`，删除秘钥时改用 `setSecret(key, "")` 覆盖清空。

## 修正：选择秘钥对话框改为显示 Obsidian 钥匙串真实数据

- 发现 Obsidian `SecretStorage` 实际提供 `listSecrets(): string[]`（1.11.4+），可直接枚举钥匙串中全部 key。
- 重写 `src/token-manager.ts`：移除自维护的 `tokenProfiles`，改为直接调用 `listSecrets()`/`getSecret()`/`setSecret()`。
- `src/settings.ts`：`TokenSelectModal` 数据源改为 `tokenManager.listSecrets()`，显示钥匙串中的实际 key ID（如 `gitee-sync-plus-gitee`）。
- `src/main.ts`：加载设置后从当前选中的 key 恢复内存 token；`migrateTokensToKeychain()` 委托给 `TokenManager.migrateLegacyTokens()`。
- `src/i18n.ts`：文案从"名称/令牌"改为"密钥 ID/令牌值"，并增加空列表/无匹配/保存失败提示。
- 构建通过，安全扫描无禁用 API。

## 阶段四合规修复（按 obsidian-plugin-dev 审查结果）

- `manifest.json`：`author` 由 `eric` 改为 `hellokunzai`（匹配 GitHub 仓库 owner，修正上游 fork 遗留）。
- `LICENSE`：保留 `ericquan8` 版权行，新增 `Copyright (c) 2026 hellokunzai`（fork 合规保留原作者署名）。
- `src/i18n.ts`：新增 `t(key)` 别名函数，匹配 skill 合规检查"导出 t() 函数"；既有 `messages().key` 用法不变。
- `scripts/release.sh`：`git push` → `git push github`、`gh release create` 增加 `styles.css` 附件并加 `--repo hellokunzai/obsidian-gitee-sync-plus`，防止误推到 origin(ericquan8 上游)。
- 全部改动停留在工作区，未 git 提交（遵循死命令）。构建通过。

## 发布 1.0.6（GitHub Actions 自动构建）

- 用户确认本次发布为 **1.0.6**（manifest 原已是 1.1.0，按用户选择降回 1.0.6 视作补丁版）。
- 版本号回落：`manifest.json` / `package.json` 1.1.0 → 1.0.6；`versions.json` 删 1.1.0 条目、加 `1.0.6 → 1.8.7`；`CHANGELOG.md` 标题 1.1.0 → 1.0.6。
- 提交拆分（遵循 AGENTS.md）：commit `7e18f36` 收口合规修复（LICENSE/i18n/release.sh/记忆）；commit `aded440` "Release 1.0.6" 仅含版本号变更；功能改动在既有 `f1c576f`。
- `git tag 1.0.6` → `git push github main 1.0.6`（SSH 别名 github.com-hellokunzai，443 端口）→ 触发 `.github/workflows/release.yml`（`on: push: tags: '*'`）。
- 经 GitHub API 确认 release 已发布：非 draft、非 prerelease，资产 `main.js`(148KB)/`manifest.json`/`styles.css` 全部上传成功。
- 注：本机 `git status` 显示 "upstream is gone" 是 Git-for-Windows 跟踪引用不持久化的已知现象（见全局记忆），推送本身已成功，无需处理。
