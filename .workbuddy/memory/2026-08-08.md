- 修改默认 .gitignore 内容：去掉重复的 .git/、.trash/、/.obsidian/ 前缀斜杠项，精简为用户指定版本
- 修改分支下拉框行为：打开设置页不再自动请求 API，点击刷新按钮才加载；去掉 disabled 逻辑，未查询到分支时显示空下拉框；新增 5 分钟内存缓存
- 插件名称从 "Gitee Sync" 改为 "Gitee Sync Plus"（manifest.json + README.md + i18n.ts 诊断日志标题 + gitignore.ts 管理块注释）
- 插件 ID 从 gitee-sync 改为 gitee-sync-plus，涉及 manifest.json、package.json、package-lock.json、README.md、AGENTS.md、scripts/release.sh、以及 src 下所有引用（main.ts sync state key + console prefix、sync.ts log file name、gitignore.ts managed markers + default gitignore、i18n.ts 中英文描述）
- ⚠️ breaking change：已有用户的插件目录从 .obsidian/plugins/gitee-sync/ 变为 .obsidian/plugins/gitee-sync-plus/，设置和同步基线不会自动迁移
- 生成本轮改动总结文档 CHANGELOG.md（涵盖 a767fe8..461ff81 共 6 次提交，13 文件 523+/125-）
- 基于 CHANGELOG 用卡兹克风格写公众号文章 article-1.0.0-release.md（搬家比喻切入），四层自检全部通过
- 第二版公众号文章 article-1.0.0-v2.md（三个自己打脸的设计决策切入，角度区别于 v1），修复2处禁用词后四层自检全部通过
- 第三版公众号文章 outputs/gitee-sync-plus-1.0.0-公众号文章.md，用 9 段式通用模板生成，覆盖开篇导语、.gitignore 重构、分支下拉框、品牌升级、安装指南、FAQ、总结、互动结尾，配图位置标注 5 处
- 备份模板版为 gitee-sync-plus-1.0.0-公众号文章-v1-模板版.md
- 第四版：融入用户个人故事重写——墨光批注插件 → .obsidian-annotations 无法同步 → Gitee Sync 是唯一选择（简单/双平台/不依赖git）→ fork 改 .gitignore → 发现思路与原插件背道而驰 → 新开 Gitee Sync Plus 项目。叙事线：发现问题→调研选型→动手改造→理念分歧→独立发展，文章风格从功能公告转为个人经历+技术分享
- 新增批量提交功能：设置页新增「提交模式」下拉框（每文件单独提交 / 合并为一次提交）。batch 模式下 Phase 2 所有 push + remoteDelete 合并为一个 commit。Gitee 用 POST /repos/{owner}/{repo}/commits（actions 数组），GitHub 用 Git Database API（blobs→trees→commits→refs，force:false 做 fast-forward 保护）。改动涉及 i18n.ts、settings.ts、backend.ts、githost.ts、sync.ts，构建通过。
- 新增侧边栏 Git 面板（src/GitPanelView.ts，仿 IDE 源控制栏，不依赖本地 git 程序）：ribbon 图标 + 命令「打开 Git 面板」打开右侧栏视图。显示同步计划分「本地更改 / 远端更改」两组，带状态徽章（M/U/D/↓）和数量；提交说明输入框（Ctrl+Enter 提交，批量模式生效）；提交（pushLocal）/拉取（pullRemote）/刷新按钮；本地更改支持逐文件与「放弃全部」放弃更改（DiscardConfirmModal 二次确认，覆盖远端版本/删除本地新增）。同步引擎重构：run() 拆为 executePull/executePush 复用，新增 computePlan/pullRemote/pushLocal(message?)/discardChanges(plan,paths)；batchCommit 增加可选 message 参数（githost.ts + backend.ts 接口）。main.ts 注册视图 + activateGitPanel() + announceSummary()。构建通过，产物已拷到 D:\WorkSprace\gitee\build。
- 新增差异对比视图（src/DiffView.ts）：点击 Git 面板中任意文件行，在主区域打开并排 diff。根据变更方向自动选择对比双方（本地修改=远端↔本地，本地新增=空↔本地，本地删除=远端↔空，远端修改=本地↔远端，远端删除=本地↔空）。行级高亮等/增/删/改，含行号、列标签、二进制文件提示。GitPanelView.ts 文件行改为可点击并阻止放弃按钮冒泡；main.ts 注册 gitee-sync-plus-diff 视图；i18n.ts + styles.css 补充文案样式。构建通过并 git 提交。
- 修复差异视图卡在「正在加载差异…」的问题：Obsidian 中 setState() 早于 onOpen() 调用，此时 diffContentEl 未创建，loadDiff() 访问 undefined 导致未捕获异常。修复：setState 仅在 opened=true 后触发加载；onOpen 创建 DOM 并设置 opened 再加载；loadDiff/renderDiff 增加 diffContentEl 存在性保护；错误处理增加 console.error。构建通过并 git 提交。
- 新增差异块还原功能：在 DiffView 中每个差异块的首个差异行右侧 gutter 显示「还原」按钮（hover 浮现），点击后二次确认，仅回退当前差异块而非整个文件。实现：DiffRow 增加 chunkIndex，computeDiffRows 给连续非 equal 行分配 chunk 编号；DiffView 保存 currentRows/currentBeforeText/currentAfterText；buildTextWithChunkReverted 根据变更方向识别本地侧，按行号范围替换目标文本中的块；新增 RevertConfirmModal。i18n.ts 和 styles.css 补充文案/样式。构建通过并 git 提交 `50d0563`。
- 优化差异块还原按钮 UI：图标从 ⟲ 改为 ↩（回退箭头），按钮改为 18px 圆形并右对齐在行号 gutter 内，hover 时淡入、悬停按钮本身显示强调色边框。构建通过并 git 提交 `8bda039`。
- 新增 Git 面板「暂存更改」功能：本地更改拆分为「暂存的更改」和「更改」两组，每个文件行前增加复选框，勾选即暂存；分组标题旁提供「全部暂存 / 全部取消暂存 / 放弃全部」按钮。提交时只提交已暂存的文件，其余本地改动保留；提交成功后自动清空暂存区和提交说明。实现：sync.ts 中 pushLocal(message?, staged?) 增加 staged 路径白名单，executePush 按 staged 过滤 plan.pushes 和 plan.remoteDeletes；GitPanelView.ts 新增 staged Set 并拆组渲染；i18n.ts + styles.css 补充暂存相关文案与样式。构建通过并 git 提交 `e9bcbfa`。
- 修复 Git 面板「提交」两个 bug：① commit message 不生效——默认 commitMode=single（逐文件提交）时，executePush 的 single 分支调用 backend.upload 完全没传 message，upload 内部用自动生成的 commitAdd/update(path) 文案，用户写在输入框的消息被丢弃。修复：backend.upload 的 opts 增加可选 message 字段；githost.ts upload 在 message 非空时用用户消息否则 fallback 默认文案；executePush single 分支透传 message。② 提交/拉取后面板不刷新导致"暂存区没清空"的视觉假象——onCommit/onPull 在 this.busy=true 时同步调用 refresh()，被 refresh 的 `if(this.busy) return` 守卫跳过，staged 集合虽已清空但 UI 未重绘。修复：将 refresh() 移到 finally 之后（busy 释放后）调用。构建通过，产物已拷到 D:\WorkSprace\gitee\build。
- Git 面板图标 VS Code 风格优化（用户参考 VS Code 源控制栏截图，但模型不支持图片，按描述推断实现）：① 文件行「放弃」文字按钮改为撤销箭头图标(↩)，默认隐藏、hover 行才浮现；② 分组标题操作改为图标——全部暂存(+)/全部取消暂存(−)/放弃全部(↩)，分组按钮常驻可见；③ 状态徽章去掉背景药丸，改为 VS Code 风格的纯彩色字母(M/U/D)。新增 .gitee-sync-plus-panel-icon-btn 与 .gitee-sync-plus-panel-item-action(hover 显隐) 样式，废弃 .gitee-sync-plus-panel-mini-btn。构建通过并 git 提交 `e7b2a7c`，产物已拷到 D:\WorkSprace\gitee\build。
- Git 面板暂存按钮从复选框改为 +/- 图标：文件行左侧不再显示 checkbox，未暂存行显示 `+` 图标按钮点击暂存，已暂存行显示 `-` 图标按钮点击取消暂存；分组标题「全部取消暂存」图标同步改为 ASCII `-`；图标默认隐藏，悬停行时浮现。实现：GitPanelView.ts 给 renderGroup 增加 mode 参数区分 staged/unstaged 组并渲染对应图标；styles.css 移除 checkbox 样式，新增 .gitee-sync-plus-panel-stage-btn。构建通过，产物已拷到 D:\WorkSprace\gitee\build，git 提交 `50592cb`。
- Git 面板图标移到文件行右侧 + 路径截断：① 文件行布局调整为「状态徽章 + 路径 +（+/- 暂存图标）+（↩ 放弃图标）」，所有操作图标统一在右侧；② 路径超出容器宽度时单行显示并用 ... 截断（white-space:nowrap + overflow:hidden + text-overflow:ellipsis + min-width:0）；③ 图标按钮加 flex:0 0 auto 防止被弹性路径挤压。GitPanelView.ts 将 stage/unstage 图标节点挪到 path 之后；styles.css 调整 .gitee-sync-plus-panel-item-path 与 .gitee-sync-plus-panel-icon-btn，移除无用 .gitee-sync-plus-panel-stage-btn。构建通过，产物已拷到 D:\WorkSprace\gitee\build，git 提交 `aaab5e8`。
- Git 面板新增「查看」图标、移除整行点击打开 diff：① 文件行右侧新增 👁 查看图标按钮（hover 显示），仅点击它才打开差异对比视图；② 移除 `row.addEventListener("click", openDiff)` 整行点击行为与 `panelClickToDiff` 悬停提示，避免误触；③ i18n.ts 新增 panelViewDiff（中/英「查看差异」）。文件行最终布局：状态徽章 + 路径 +（+/- 暂存）+（↩ 放弃）+（👁 查看）。构建通过，产物已拷到 D:\WorkSprace\gitee\build，git 提交 `650231b`。
- Git 面板文件行右侧图标顺序调整为「查看 → 放弃 → 暂存」（DOM 创建顺序改动）：👁 查看（最靠近文件名）→ ↩ 放弃 → +/- 暂存（最外侧）。git 提交 `e0e05a5`，产物已拷到 D:\WorkSprace\gitee\build。
- Git 面板「查看」图标改用文件图标：用户反馈 👁 emoji 突兀，改为用 Obsidian 内置 `setIcon(viewBtn, "file-text")` 渲染 lucide 的文件图标（文件带横线，表示查看文件内容），与其它线条风格图标更统一。GitPanelView.ts import 增加 setIcon，移除 emoji text；styles.css 新增 `.gitee-sync-plus-panel-icon-btn svg { width:15px; height:15px }` 让 SVG 与字符图标对齐。git 提交 `e6ffa5a`，产物已拷到 D:\WorkSprace\gitee\build。
