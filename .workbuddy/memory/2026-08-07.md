- 将设置界面的"分支"字段从文本输入框改为动态下拉框
- 新增 `fetchBranches()` 函数（githost.ts），通过 Gitee/GitHub API 拉取分支列表
- 信息齐全：自动获取分支列表，显示可编辑下拉框 + 刷新按钮
- 信息不全 / API 失败：显示禁用下拉框（显示当前值但不能选）+ 刷新按钮，附提示文案
- i18n 新增 `settingsBranchNeedInfo`、`settingsBranchLoadFailed`、`refreshBranches`
- 构建通过（tsc + esbuild），产物复制到 D:\WorkSprace\gitee\build
- 调整设置界面四个配置项顺序，Gitee 和 GitHub 统一为：私人令牌/Token → 用户名 → 仓库名 → 分支
